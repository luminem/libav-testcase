/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'federico' at '11/01/16 16:20' with Gradle 2.5
 *
 * This generated file contains a commented-out sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at http://gradle.org/docs/2.5/userguide/tutorial_java_projects.html
 */


// Apply the java plugin to add support for Java
apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'application'

import org.gradle.internal.os.OperatingSystem;

// In this section you declare where to find the dependencies of your project
repositories {
    mavenLocal()
}

/*// In this section you declare the dependencies for your production and test code
dependencies {
    // The production code uses the SLF4J logging API at compile time
    compile 'org.slf4j:slf4j-api:latest.release'

    // Declare the dependency for your favourite test framework you want to use in your tests.
    // TestNG is also supported by the Gradle Test task. Just change the
    // testCompile dependency to testCompile 'org.testng:testng:6.8.1' and add
    // 'test.useTestNG()' to your build script.
    testCompile 'junit:junit:latest.release'
}
*/


dependencies {
    compile 'org.bytedeco.javacpp-presets:libav:12-1.2-SNAPSHOT'
    if (OperatingSystem.current().isMacOsX()) {
        compile 'org.bytedeco.javacpp-presets:libav:12-1.2-SNAPSHOT:macosx-x86_64'
    }
    if (OperatingSystem.current().isLinux()) {
        compile 'org.bytedeco.javacpp-presets:libav:12-1.2-SNAPSHOT:linux-x86_64'
    }
//    compile 'org.bytedeco.javacpp-presets:libav:12-1.2-SNAPSHOT:windows-x86_64'

}

mainClassName = "it.luminem.LibavFrameRecorder"
